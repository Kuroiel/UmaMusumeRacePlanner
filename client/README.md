# UmaRace

Site to keep track of Uma races for affinity

To view test results: Click the 'Actions' tab in this repository to see the CI/CD logs.
To run locally: Clone the repo, run npm install, then run npx playwright test.
